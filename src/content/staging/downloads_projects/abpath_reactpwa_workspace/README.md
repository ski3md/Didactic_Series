# ABPath ReactPWA Workspace

This repository does not contain reusable educational content for the requested migration categories.

## Result

- `algorithms`: none found
- `lectures`: none found
- `tutorials`: none found
- `images`: only `public/favicon.svg`

## Notes

- The repo is mostly a small React/Vite PWA shell with a service worker and HTTPS dev setup.
- The only declared package dependency in the lockfile is `openai`.
- No raw lesson banks, lecture JSON, or tutorial datasets were present to stage.
