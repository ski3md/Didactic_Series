# Migration Summary

`micro_tutor_4` is primarily a Next.js tutoring scaffold. It does not contain standalone lecture or algorithm libraries, but it does include embedded tutorial seed content in the main page and the question-generation API route.

The only clear raw asset is `public/logo.svg`. Everything else is application code, UI, or backend wiring for generating adaptive learning questions.

Recommended next step: copy the embedded demo question and prompt templates only if you want seed content for migration. Otherwise, this repo should stay out of the main content import path.
